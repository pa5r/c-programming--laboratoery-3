#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a<=b)
    {
        printf("smallest numder");
    }
    else
    {
        printf("largest number");
    }
    
    return 0;
}

